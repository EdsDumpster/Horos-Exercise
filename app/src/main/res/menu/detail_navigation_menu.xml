<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_daily"
        android:icon="@drawable/day_icon"
        android:title="@string/daily_navigation_menu"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_weekly"
        android:icon="@drawable/week_icon"
        android:title="@string/weekly_navigation_menu"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_monthly"
        android:icon="@drawable/month_icon"
        android:title="@string/monthly_navigation_menu"
        app:showAsAction="always"/>
</menu>